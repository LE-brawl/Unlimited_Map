import { NextResponse } from "next/server";
import { createAssetFromFile, createAssetFromUrl } from "@/lib/ai/tokenstar/tokenstarAsset";
import { normalizeAIError } from "@/lib/ai/errors";
const valid = (value: unknown): value is "Image" | "Video" | "Audio" => ["Image", "Video", "Audio"].includes(String(value));
export async function POST(request: Request) { try { if (request.headers.get("content-type")?.includes("multipart/form-data")) { const form = await request.formData(); const groupId = form.get("groupId"), name = form.get("name"), assetType = form.get("assetType"), file = form.get("file"); if (typeof groupId !== "string" || typeof name !== "string" || !valid(assetType) || !(file instanceof Blob)) return NextResponse.json({ ok: false, error: { message: "groupId, name, valid assetType, and file are required.", status: 400 } }, { status: 400 }); return NextResponse.json({ ok: true, output: await createAssetFromFile({ groupId, name, assetType, file }) }); } const body = await request.json() as { groupId?: unknown; name?: unknown; assetType?: unknown; url?: unknown }; if (typeof body.groupId !== "string" || typeof body.name !== "string" || typeof body.url !== "string" || !valid(body.assetType)) return NextResponse.json({ ok: false, error: { message: "groupId, name, url, and valid assetType are required.", status: 400 } }, { status: 400 }); return NextResponse.json({ ok: true, output: await createAssetFromUrl({ groupId: body.groupId, name: body.name, assetType: body.assetType, url: body.url }) }); } catch (error) { const e = normalizeAIError(error); return NextResponse.json({ ok: false, error: e }, { status: e.status }); } }
