import { NextResponse } from "next/server";
import { normalizeAIError } from "@/lib/ai/errors";
import { pollKlingVideo, pollSeedanceVideo } from "@/lib/ai/tokenstar/tokenstarVideoProvider";
export async function POST(request: Request) { try { const body = await request.json() as { taskId?: unknown; pollAction?: unknown }; if (typeof body.taskId !== "string" || !body.taskId) return NextResponse.json({ ok: false, error: { message: "taskId is required.", status: 400 } }, { status: 400 }); return NextResponse.json({ ok: true, provider: "tokenstar", output: typeof body.pollAction === "string" && body.pollAction ? await pollKlingVideo(body.taskId, body.pollAction) : await pollSeedanceVideo(body.taskId), polling: { intervalMs: Number(process.env.TOKENSTAR_POLL_INTERVAL_MS || 12000) } }); } catch (error) { const e = normalizeAIError(error); return NextResponse.json({ ok: false, error: e }, { status: e.status }); } }
