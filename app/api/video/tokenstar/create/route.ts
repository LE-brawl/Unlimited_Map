import { NextResponse } from "next/server";
import { normalizeAIError } from "@/lib/ai/errors";
import { createKlingImageVideo, createKlingOmniVideo, createKlingTextVideo, createSeedanceAssetVideo, createSeedanceVideo } from "@/lib/ai/tokenstar/tokenstarVideoProvider";
export async function POST(request: Request) { try { const body = await request.json() as Record<string, unknown>; if (typeof body.prompt !== "string" || !body.prompt.trim()) return NextResponse.json({ ok: false, error: { message: "prompt is required.", status: 400 } }, { status: 400 }); const urls = (value: unknown) => Array.isArray(value) ? value.filter((item): item is string => typeof item === "string") : undefined; const input = { prompt: body.prompt, model: typeof body.model === "string" ? body.model : undefined, image: typeof body.image === "string" ? body.image : undefined, video: typeof body.video === "string" ? body.video : undefined, ratio: typeof body.ratio === "string" ? body.ratio : undefined, duration: typeof body.duration === "number" ? body.duration : undefined, resolution: typeof body.resolution === "string" ? body.resolution : undefined, generateAudio: typeof body.generateAudio === "boolean" ? body.generateAudio : undefined, klingElementId: typeof body.klingElementId === "string" ? body.klingElementId : undefined, referenceImageUrls: urls(body.referenceImageUrls), referenceVideoUrls: urls(body.referenceVideoUrls), referenceAudioUrls: urls(body.referenceAudioUrls), referenceImageAssetUrl: typeof body.referenceImageAssetUrl === "string" ? body.referenceImageAssetUrl : undefined, referenceVideoAssetUrl: typeof body.referenceVideoAssetUrl === "string" ? body.referenceVideoAssetUrl : undefined, referenceAudioAssetUrl: typeof body.referenceAudioAssetUrl === "string" ? body.referenceAudioAssetUrl : undefined }; const output = body.mode === "kling-text-to-video" ? await createKlingTextVideo(input) : body.mode === "kling-image-to-video" ? await createKlingImageVideo(input) : body.mode === "kling-omni" ? await createKlingOmniVideo(input) : body.mode === "asset-video" ? await createSeedanceAssetVideo(input) : await createSeedanceVideo(input); return NextResponse.json({ ok: true, provider: "tokenstar", output }); } catch (error) { const e = normalizeAIError(error); return NextResponse.json({ ok: false, error: e }, { status: e.status }); } }
